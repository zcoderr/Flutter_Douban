import 'package:flutter/material.dart';

class UpComningPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Text('ss'),
    );
  }
}
